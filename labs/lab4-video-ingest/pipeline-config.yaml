# Sample pipeline configuration
# Before deploying, replace rbansal2 with your username (`echo rbansal2`) and <kubernetes-cluster-vrn> with your cluster VRN.
name: rbansal2-video-ingest-pipeline
description: S3-triggered pipeline that segments uploaded .mp4 files into 5-second clips
kubernetes_cluster_vrn: <kubernetes-cluster-vrn>
namespace: default
manifest:
  config:
    environment_variables:
      - name: S3_ENDPOINT_URL
        value: "http://<your-s3-endpoint>"
      - name: S3_REGION
        value: "<your-region>"
      - name: OUTPUT_BUCKET
        value: "rbansal2-video-segments"
      - name: SEGMENT_DURATION
        value: "5"
      - name: VLM_ENDPOINT
        value: "<your-vlm-endpoint>"
      - name: VISION_MODEL
        value: "<your-vision-model>"
      - name: EMBEDDING_MODEL
        value: "<your-embedding-model>"
      - name: SUMMARY_MODEL
        value: ""
      - name: MAX_TOKENS
        value: "512"
      - name: VASTDB_ENDPOINT
        value: "<your-vastdb-endpoint>"
      - name: VASTDB_BUCKET
        value: "<your-vastdb-bucket>"
      - name: VASTDB_SCHEMA
        value: "video_embeddings"
      - name: VASTDB_TABLE
        value: "segments"
  triggers:
    - name: rbansal2-s3-segment-video-trigger
      vrn: vast:dataengine:triggers:rbansal2-s3-segment-video-trigger
  function_deployments:
    - name: rbansal2-s3-segment-video
      function_vrn: vast:dataengine:functions:rbansal2-s3-segment-video
      revision: 1
  links:
    - source: ["rbansal2-s3-segment-video-trigger"]
      destination: ["rbansal2-s3-segment-video"]
